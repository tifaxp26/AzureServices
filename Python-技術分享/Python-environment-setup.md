| 💡 macOS | 💡 Windows |
| -------- | ---------- |
| •**使用** `venv` **模組來建立虛擬環境**：<br>```python -m venv .venv``` | •**使用** `venv` **模組來建立虛擬環境**：<br>```python -m venv .venv``` |
| •**啟動虛擬環境**<br>使用以下指令來啟動虛擬環境：<br>```source .venv/bin/activate``` | •**啟動虛擬環境**<br>使用以下指令來啟動虛擬環境：<br>```.\.venv\Scripts\Activate.ps1``` |
|•退出虛擬環境：```deactivate```|•退出虛擬環境：```deactivate```|
|•**使用requirements建立python package:**<br>```pip install -r requirements.txt```|•**使用requirements建立python package:** <br> ```pip install -r requirements.txt```|
|•**輸出requirements：** <br> ```pip freeze > requirements.txt```|•**輸出requirements：** <br> ```pip freeze > requirements.txt```|